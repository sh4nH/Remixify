// TopTracksButton.js
import React from "react";

const TopTracksButton = ({ onClick }) => {
  return <button className="my-button" onClick={onClick}>Show my Top 10 tracks</button>;
};

export default TopTracksButton;
